# Docker Profile - Para ejecutar la aplicación dentro del contenedor de Tomcat
# Este perfil usa nombres de contenedores Docker en lugar de localhost

# Authentik Configuration (usando nombres de contenedores)
spring.security.oauth2.client.provider.authentik.issuer-uri=http://authentik-server:9000/application/o/inv/
spring.security.oauth2.client.provider.authentik.authorization-uri=https://auth.mariastaff.com/application/o/authorize/
spring.security.oauth2.client.provider.authentik.token-uri=http://authentik-server:9000/application/o/token/
spring.security.oauth2.client.provider.authentik.user-info-uri=http://authentik-server:9000/application/o/userinfo/
spring.security.oauth2.client.provider.authentik.jwk-set-uri=http://authentik-server:9000/application/o/inv/jwks/

# Authentik API Configuration (usando nombre del contenedor)
authentik.api.url=http://authentik-server:9000
authentik.api.token=inv_tk
authentik.app.slug=inv

# Database Configuration (usando nombre del contenedor de PostgreSQL)
spring.datasource.url=jdbc:postgresql://db:5432/dev_app_db
spring.datasource.username=dev_app_user
spring.datasource.password=F4cxO1xdCdKRx2VuHCh2gQ
